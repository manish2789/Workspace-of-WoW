#include "conv.h"
 /*! Here is a snapshot of my new application:
   *  \image html conv.png
   *  \image latex application.eps "My application" width=10cm
 */


void untitled ( ) {
	uint32_t *an;
	
    an = (uint32_t*)malloc(sizeof(uint32_t*) * an);
		uint32_t *;
	
     = (uint32_t*)malloc(sizeof(uint32_t*) * );
		uint32_t *bn;
	
    bn = (uint32_t*)malloc(sizeof(uint32_t*) * bn);
		uint32_t  conv = ;
		conv = [arr/output] * [arr/output]  ;
		
	conv = Conv/Convolution_result;
		
}