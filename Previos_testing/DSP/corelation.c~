#include "corelation.h"
 /*! Here is a snapshot of my new application:
   *  \image html corelation.png
   *  \image latex application.eps "My application" width=10cm
 */


void co-realation ( ) {
	uint32_t *bn;
	
    bn = (uint32_t*)malloc(sizeof(uint32_t*) * bn);
		uint32_t  co-realation_out = ;
		uint32_t *an;
	
    an = (uint32_t*)malloc(sizeof(uint32_t*) * an);
		co-realation_out = [arr/output] * [ - arr/output]  ;
		
	co-realation_out = Co-re/correlation_result;
		
}