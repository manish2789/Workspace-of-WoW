#include "demo_mul.h"
 /*! Here is a snapshot of my new application:
   *  \image html demo_mul.png
   *  \image latex application.eps "My application" width=10cm
 */


void vect_mul ( ) {
int n=5;
	int n1;
	
    a = (int*)malloc(sizeof(int*) * n1 );
		int n2;
	
    b = (int*)malloc(sizeof(int*) * n2 );
		uint32_t n3;
	
    c = (int*)malloc(sizeof(int*) * n3 );
		c = a[n] * b[n]  ;
		
	
}
