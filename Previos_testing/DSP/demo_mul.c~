#include "demo_mul.h"
 /*! Here is a snapshot of my new application:
   *  \image html demo_mul.png
   *  \image latex application.eps "My application" width=10cm
 */


void vect_mul ( ) {
	uint32_t n1;
	
    a = (uint32_t*)malloc(sizeof(uint32_t*) * n1 );
		uint32_t n2;
	
    b = (uint32_t*)malloc(sizeof(uint32_t*) * n2 );
		uint32_t n3;
	
    c = (uint32_t*)malloc(sizeof(uint32_t*) * n3 );
		arr/Name = arr/output[n] * arr/output[n]  ;
		
	
}