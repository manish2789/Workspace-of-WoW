#include "vect_mul.h"
 /*! Here is a snapshot of my new application:
   *  \image html vect_mul.png
   *  \image latex application.eps "My application" width=10cm
 */


void vect_mul ( ) {
	uint32_t a;
	
    a1 = (uint32_t*)malloc(sizeof(uint32_t*) * a );
		uint32_t out;
	
    out1 = (uint32_t*)malloc(sizeof(uint32_t*) * out );
		uint32_t b;
	
    b1 = (uint32_t*)malloc(sizeof(uint32_t*) * b );
		arr/Name = [arr/output] * [ arr/output]  ;
		
	
}