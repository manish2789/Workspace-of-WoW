#include "add.h"
 /*! Here is a snapshot of my new application:
   *  \image html add.png
   *  \image latex application.eps "My application" width=10cm
 */


void mat_add ( ) {
	uint32_t  a[] = ;
		uint32_t  b[] = ;
		uint32_t *out[];
	
    out[] = (uint32_t*)malloc(sizeof(uint32_t*) * out[]);
		uint32_t *b[];
	
    b[] = (uint32_t*)malloc(sizeof(uint32_t*) * b[]);
		uint32_t *a[];
	
    a[] = (uint32_t*)malloc(sizeof(uint32_t*) * a[]);
		uint32_t  c[] = ;
		arr/Name = [arr/output] + [arr/output] ;
       
	
}