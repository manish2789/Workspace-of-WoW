#include "scale_mat.h"
 /*! Here is a snapshot of my new application:
   *  \image html scale_mat.png
   *  \image latex application.eps "My application" width=10cm
 */


void sacle ( ) {
	arr [1];
	    arr = (*)malloc(sizeof(uint32_t*) *  );
		uint32_t  var = ;
		arr [1];
	    arr = (*)malloc(sizeof(uint32_t*) *  );
		
}