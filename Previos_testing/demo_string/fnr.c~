#include "fnr.h"
 /*! Here is a snapshot of my new application:
   *  \image html fnr.png
   *  \image latex application.eps "My application" width=10cm
 */


void fnr ( ) {
	str_find [20];
	    str_find = (*)malloc(sizeof(char*) *  );
		str_src [20];
	    str_src = (*)malloc(sizeof(char*) *  );
		str_result [20];
	    str_result = (*)malloc(sizeof(char*) *  );
		str_replace [20];
	    str_replace = (*)malloc(sizeof(char*) *  );
		arr/Array_Name = strfnr (arr/output_array,arr/output_array,arr/output_array);
		
	
}