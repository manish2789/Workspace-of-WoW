#include "string_cmp.h"
 /*! Here is a snapshot of my new application:
   *  \image html string_cmp.png
   *  \image latex application.eps "My application" width=10cm
 */


void str_cmp ( ) {
	 [20];
	     = (*)malloc(sizeof(char*) *  );
		 [1];
	     = (*)malloc(sizeof(uint32_t*) *  );
		 [20];
	     = (*)malloc(sizeof(char*) *  );
		uint32_t  reult = ;
		reult = strcmp (arr/output_array,arr/output_array,);
		
	reult = Strcmp/output;
		
}