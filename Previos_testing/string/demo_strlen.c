#include "demo_strlen.h"
 /*! Here is a snapshot of my new application:
   *  \image html demo_strlen.png
   *  \image latex application.eps "My application" width=10cm
 */


void String_length ( ) {
	uint32_t  str_len;
		const unsigned char * str;
	
    str = (const unsigned char*)malloc(sizeof(const unsigned char*));

	str_len = strlen (arr/output);
		
	
}