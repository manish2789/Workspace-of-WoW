#include "string_compare.h"
 /*! Here is a snapshot of my new application:
   *  \image html string_compare.png
   *  \image latex application.eps "My application" width=10cm
 */


void string_compare ( ) {
	uint32_t  value;
		const unsigned char *str1;
	
    str1 = (const unsigned char*)malloc(sizeof(const unsigned char*) * str1);

	const unsigned char *str2;
	
    str2 = (const unsigned char*)malloc(sizeof(const unsigned char*) * str2);

	value = strcmp (arr/output,arr/output,);
		
	
}