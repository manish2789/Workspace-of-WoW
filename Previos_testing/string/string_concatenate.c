#include "string_concatenate.h"
 /*! Here is a snapshot of my new application:
   *  \image html string_concatenate.png
   *  \image latex application.eps "My application" width=10cm
 */


void string_concate ( ) {
	const  unsigned char *str_source;
	
    str_source = (const  unsigned char*)malloc(sizeof(const  unsigned char*) * str_source);

	const  unsigned char  *str_destn;
	
    str_destn = (const  unsigned char *)malloc(sizeof(const  unsigned char *) * str_destn);

	
	
        strcat (arr/Name,"arr/output");
		
	
}