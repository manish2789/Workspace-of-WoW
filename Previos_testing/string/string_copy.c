#include "string_copy.h"
 /*! Here is a snapshot of my new application:
   *  \image html string_copy.png
   *  \image latex application.eps "My application" width=10cm
 */


void String_copy ( ) {
	const  unsigned char *str;
	
    str = (const  unsigned char*)malloc(sizeof(const  unsigned char*) * str);
		uint32_t  var;
		uint32_t *arr;
	
    arr = (uint32_t*)malloc(sizeof(uint32_t*) * arr);
		uint32_t *arr;
	
    arr = (uint32_t*)malloc(sizeof(uint32_t*) * arr);
		const unsigned char *str2;
	
    str2 = (const unsigned char*)malloc(sizeof(const unsigned char*) * str2);
		
	
        strcpy (arr/Name,arr/output);
		
	
}