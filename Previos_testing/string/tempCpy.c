#include "tempCpy.h"
 /*! Here is a snapshot of my new application:
   *  \image html tempCpy.png
   *  \image latex application.eps "My application" width=10cm
 */


void string copy ( ) {
	unsigned char * arr;
	
    arr = (unsigned char*)malloc(sizeof(unsigned char*));

	unsigned char * arr;
	
    arr = (unsigned char*)malloc(sizeof(unsigned char*));

	
	
        strcpy (arr/input,arr/output);
		
	
}