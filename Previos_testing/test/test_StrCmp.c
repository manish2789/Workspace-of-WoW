#include "test_StrCmp.h"
 /*! Here is a snapshot of my new application:
   *  \image html test_StrCmp.png
   *  \image latex application.eps "My application" width=10cm
 */


void String Comapre ( ) {
	uint32_t  value;
		*;
	
     = (*)malloc(sizeof(*));

	*;
	
     = (*)malloc(sizeof(*));

	value = strcmp (arr/output,arr/output,);
		
	
}
